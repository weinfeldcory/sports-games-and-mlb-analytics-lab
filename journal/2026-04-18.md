# 2026-04-18

## Session
Worked on the baseball analytics app viewer and roster builder. Added roster diagnostics, swap-based roster replacement, saved local roster scenarios, roster move impact notes, and a data freshness strip. Also updated the pitcher ingestion path so pitching can be refreshed independently and request the full pitcher pool, while documenting that the current local pitcher dataset is still starter-only until the source refresh succeeds.

Summary: Expanded the baseball analytics app viewer and pitching refresh workflow.
Branch: `main`
Changes before commit: updated the baseball analytics app viewer modules and `src/pull_data.py`.

## Session
Built out the `projects/mlb-attendance-app` web experience around a real attendance ledger instead of demo data. Added fetched MLB game logs for the full attended-game list, richer hitter and pitcher aggregates, sortable player tables with minimum filters and rate stats, friend-following support, and a tighter home and stats UI that also surfaces team records and every team seen.

Summary: Expanded the MLB attendance app into a real web attendance ledger.
Branch: `main`
Changes before commit: updated the `projects/mlb-attendance-app` app shell, data seed and fetch scripts, domain stats pipeline, and tests.
